接下来的任务是要确定相似的状态，需要在程序中确定相似的状态，不太可能把整个存储牌局进行信息的对象都存储下来，
然后比较两个对象，因为这样的话无法存储该状态信息。因此需要对牌局进行信息的对象进行编码。编码的方式是每到一轮，
就将之前轮的数据进行general信息编码，而对于当前轮的信息，先直接进行严格编码。用这种方式先试试两人局的情况。
而将牌局进行信息的对象进行编码，需要使用到牌局信息，因此这个编码的编程可以单开一个类，然后把牌局进行信息对象传给
这个类，这样的话也方便从中提取信息。但是这样的话不能把编码类做为成员给信息对象，不可取。因为每次信息类更新一个状态，就需要
进行一次编码。那么方案这么弄，编码的结果作为一个类，在信息对象中添加编码函数，该函数返回编码类，这样的话编码类可以方便的
提取信息。
首先只考虑翻牌后，对于翻牌后的每一次action之前，都进行一次编码，这个编码要存储起来。经过这一步之后，牌局信息中又增加了一项
翻牌后状态编码信息。编码信息单独存储为一项，每一轮的编码都存储为一个数组，这样编码的位置与action的位置正好一一对应，每一个action
的对应位置的编码即为该action进行决策时的状态。
编码存储为一个字符串，先写完上面的代码吧。

发现问题，有些后手记录错误。


35357006093039820170318021453  action error, but not recognized
